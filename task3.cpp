#include <stdio.h>
#include <string.h>
#include <conio.h>
#define SIZE 256

int getMaxWord(char buf[], char word[])

{
	int max = 0;

	char *tmp1, *max_s, *simv = "., ";

	tmp1 = strtok(buf, simv);

	int i = 0;

	return 0;
}